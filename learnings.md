## What works
- **Exploiting kernel/data symmetry to reduce operation count:** Identifying mathematical symmetries and using algebraic identities to eliminate large fractions of work. `inline-symmetric-convolutions` pre-combined symmetric neighbor pixels and reduced 36 multiplications to 16 per channel (~11.7% improvement).
- **Replacing temporary arrays with scalar variables:** `strip-internal-array-allocations-for-find_gradients-normalizations` stripped internal array allocations into native scalars, yielding ~11.9% improvement. The pattern: find small fixed-size arrays used as intermediaries, replace with individual scalar variables to help the compiler use registers.
- **Targeting the largest time consumers:** Both successful optimizations targeted the inner per-pixel loop where small per-pixel savings multiply across millions of pixels. The biggest benchmark entries (e.g., 57-66s user time) dominate total runtime.
- **Preserving identical floating-point evaluation order:** Successful optimizations must produce bit-identical results by not reordering multiply-accumulate sequences. This is a strict prerequisite for passing benchmarks.

## What to avoid
- **Inlining non-local pixel access:** Eliminating stack-allocated neighbor arrays in favor of direct memory fetches causes obvious regressions (`inline-neighbor-pixel-access`). Bulk copying to stack arrays is faster than repeated reads from L1 cache.
- **Inlining derivative accumulation:** Eliminating stack-allocated arrays for accumulation by directly modifying accumulators inline (`inline-derivative-accumulation`) increased execution time by 2.3%, likely due to increased register pressure.
- **Over-merging loops / pairing function calls:** Grouping `compute_convolution` calls into fewer loops caused obvious regressions (`pair-4-compute_convolution-calls-into-2-for_each_channel-loops`), likely due to increased loop overhead and register pressure.
- **Changing floating-point evaluation order:** Pre-combining or rearranging multiply-accumulate operations changes rounding sequences and causes benchmark errors. `fuse-paired-kernel-convolutions` failed by mathematically combining kernels before convolution.
- **Micro-optimizations that save minimal instructions:** Replacing simple arithmetic with mathematical identities (e.g., `simplify-b22-trace-identity` replacing addition with a trigonometric identity) caused obvious regressions by disrupting compiler instruction scheduling.
- **Restructuring code without algorithmic reduction:** Inlining matrix construction with algebraically reduced scalars (`rewrite-9-element-kernel-matrix-instantiation`) regressed on current workloads, suggesting it interfered with auto-vectorization. Fusing angle and exponential computations (`Fused-angle-ex`) caused an 8.1% regression — merging transcendental function calls without reducing total operation count hurts rather than helps.
- **Conditional branching to skip work inside tight loops:** Adding conditionals to skip computations causes regressions or minimal improvements that don't justify the branch overhead. `skip-zero-speed-orders` caused benchmark errors. `skip-exp-for-isotropic-orders` caused a 1.9% regression. `copy-matched-anisotropy-c2-to-skip-expf` gave a negligible 1.1% improvement, failing to meet the 5% threshold.
