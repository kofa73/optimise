remove 0.5f central-difference scaling from gradient/laplacian computation.

The factor cancels in angle ratios (cos²θ = gx²/m²) and is absorbed into `half_anisotropy[k] = anisotropy[k] * 0.5f` precomputed outside the pixel loop. Saves 4 float multiplies per pixel per channel.

outcome: not applicable

This optimization has already been applied. The most recent commit on this branch (`14fd5dbfc3`) is titled "perf: remove 0.5f scaling from gradient/laplacian computation." — exactly this change.

The code at lines 1119-1120 and 1158-1159 already computes gradients/laplacians without the 0.5f factor (`grad_x = lf7 - lf1` etc.), and lines 1037-1042 already pre-compute `half_anisotropy` to absorb the removed scaling factor.
