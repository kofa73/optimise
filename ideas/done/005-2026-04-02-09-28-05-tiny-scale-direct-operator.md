perf: replace repeated PDE iterations with a direct tiny-scale operator on coarse isotropic levels

When the wavelet pyramid reaches very small full-frame scales on the CPU path, replace the generic 17-step iterative update with a compact direct helper for the tiny isotropic grid, using precomputed coefficients for the fixed radius/order combination. This is different from merely skipping negligible scales: it still computes the same coarse contribution, but avoids spending many generic iterations on images where the stencil has collapsed to a tiny state space dominated by borders. The expected gain is smaller than the ideas above, but it attacks pure wasted work in the tail of the pyramid and should especially help presets with many iterations.
outcome: not applicable

The current CPU path in [src/iop/diffuse.c](/workspace/darktable/src/iop/diffuse.c#L3472) no longer has a separate fixed “17-step” per-scale PDE iteration to replace. `iterations` repeats the entire wavelet decomposition/reconstruction loop, and each non-negligible scale invokes `heat_PDE_diffusion()` once during reconstruction ([src/iop/diffuse.c](/workspace/darktable/src/iop/diffuse.c#L3229), [src/iop/diffuse.c](/workspace/darktable/src/iop/diffuse.c#L3292)). Because the implementation has already been restructured around one-pass per-scale PDE solves plus existing CPU specializations in [src/iop/diffuse.c](/workspace/darktable/src/iop/diffuse.c#L2744), the requested “replace repeated PDE iterations with a direct tiny-scale operator” does not map to a current hot path without introducing a new algorithm outside the described change. No edits made.
